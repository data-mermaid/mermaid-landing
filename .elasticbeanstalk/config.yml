branch-defaults:
  main:
    environment: Mermaid-landing-staging-env
  dev:
    environment: Mermaid-landing-staging-env
global:
  application_name: mermaid-landing-staging
  default_ec2_keyname: i-0985020765a3721f3
  default_platform: PHP 8.0 running on 64bit Amazon Linux 2/3.3.5
  default_region: us-east-1
  sc: git
deploy:
  artifact: main.zip
